# PropFlow 開発時の必須確認

機能の追加・変更・削除を行う際は、実装とテストだけで完了にせず、必ず紹介ページ（`client/public/propflow-intro.html`）とヘルプページ（`client/public/support.html`）を確認すること。

確認項目：

1. 既存の説明・画面名・ボタン名・制限事項が古いまま残っていないか。
2. 追加・変更した機能を紹介ページまたはヘルプページへ掲載すべきか。
3. 廃止済み・重複・利用価値が低くなった機能や説明を削除すべきか。

必要な更新は同じ作業内で行い、更新不要と判断した場合も、作業完了時に上記3項目を確認済みであることを報告すること。

## 複数PCでのGitHub同期

メインPC・サブPCのどちらで作業する場合も、GitHubの `main` をコードの正本として扱うこと。

### 作業開始時

1. `git status` で未コミット変更の有無を確認する。
2. `git fetch origin` を実行し、ローカルブランチと `origin/main` の差分を確認する。
3. 作業ツリーがクリーンで、安全にfast-forwardできる場合だけ `git pull --ff-only origin main` を実行する。
4. 未コミット変更、分岐、競合がある場合は、自動で破棄・上書き・強制同期せず、状況を報告する。

### 作業終了時

1. 変更ファイルと未追跡ファイルを確認し、依頼と無関係な変更をコミット対象から除外する。
2. コードを変更した場合は、型チェック、既存テスト、ビルド、変更箇所に必要なテストを実施する。
3. テスト成功後、依頼に関係するファイルだけを指定してコミットする。`git add .` は使用しない。
4. `git fetch origin` を実行し、ローカルと `origin/main` の状態を再確認する。
5. `main` へのpushは、ユーザーが「デプロイして」など本番反映を明示的に指示した場合だけ実行する。
6. pushが拒否された場合、force pushは行わない。原因を確認し、安全なrebaseまたはユーザーへの報告を行う。
7. 完了報告には、テスト結果、コミットID、GitHubへのpush状態、Railwayのデプロイ確認状態、残る未コミット変更を明記する。

未完成の変更を別PCへ引き継ぐ必要がある場合は、`main` へpushせず、ユーザーの了承を得て作業ブランチへコミット・pushすること。
