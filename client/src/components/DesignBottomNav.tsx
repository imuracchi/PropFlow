import { Building2, Heart, MessageCircle, Search, UserRound } from "lucide-react";
import { useLocation } from "wouter";

export default function DesignBottomNav({active}:{active:'properties'|'favorites'|'messages'|'mine'|'mypage'}){const [,go]=useLocation();const items=[{id:'properties',Icon:Search,label:'物件一覧',path:'/design'},{id:'favorites',Icon:Heart,label:'お気に入り',path:'/design/favorites'},{id:'messages',Icon:MessageCircle,label:'DM',path:'/design/messages'},{id:'mine',Icon:Building2,label:'自社物件',path:'/design/my-properties'},{id:'mypage',Icon:UserRound,label:'マイページ',path:'/design/mypage'}] as const;return <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-[#dfe3e8] bg-white pb-[max(7px,env(safe-area-inset-bottom))] pt-2"><div className="mx-auto flex max-w-md justify-around">{items.map(({id,Icon,label,path})=><button key={id} onClick={()=>go(path)} className={`relative flex w-16 flex-col items-center gap-1 text-[10px] ${active===id?'font-bold text-[#173f70]':'text-[#718096]'}`}><Icon size={21}/>{label}{id==='messages'&&<span className="absolute right-4 top-0 size-2 rounded-full bg-[#d84b4b] ring-2 ring-white"/>}</button>)}</div></nav>}
